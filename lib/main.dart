import 'package:flutter/material.dart';
import 'package:slash/my_app.dart';

void main() {
  // Keeping the main as clean as possible so that it won't be crowded with
  // things like firebase or DI initialization or other things..
  runApp(const MyApp());
}
